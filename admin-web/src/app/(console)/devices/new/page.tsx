import Link from 'next/link';
import { createDeviceAction } from '../../actions';

export default function NewDevicePage() {
  return <><header className="page-header"><div><div className="eyebrow">PROVISION DEVICE</div><h1>Add a sensor</h1><p>The device starts unassigned. A caregiver will link it to a patient from the mobile app.</p></div><Link href={'/devices' as any}>Cancel</Link></header><section className="panel form-panel"><form action={createDeviceAction} className="form-grid"><label>Serial<input name="serial" required /></label><label>Firmware version<input name="firmwareVersion" required placeholder="2.4.1" /></label><label>Room label<input name="roomLabel" required placeholder="Bedroom 1" /></label><label>Vendor<input name="vendor" placeholder="AeroSense" /></label><label>Device type<select name="deviceType" defaultValue="sleep_sensor"><option value="sleep_sensor">Sleep / vital sensor</option><option value="fall_sensor">Fall sensor</option></select></label><label>Transport<select name="transport" defaultValue="aerosense_tcp"><option value="aerosense_tcp">AeroSense TCP</option><option value="mqtt">MQTT</option></select></label><label className="wide">AeroSense radar ID / external ID<input name="externalId" placeholder="Required for AeroSense hardware" /></label><button className="primary-link wide" type="submit">Create unassigned device</button></form></section></>;
}
